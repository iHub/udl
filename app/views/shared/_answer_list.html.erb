<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover table-condensed">
    <thead>
      <tr>
        <th>#</th>
        <th>Answer</th>
        <th>Description</th>
        <th>Created</th>
        <th>Annotations</th>
      </tr>
    </thead>

    <tbody>
      <% counter = 0 %>
      <% @answers.each.with_index do |answer, index| %>
        <% counter +=1 %>
        <tr>
          <td><%= index+1 %></td>
          <td>
              <strong><%= link_to answer.answer, scrape_session_question_answer_path(@scrape_session.id, @question, answer) %><strong>
          </td>
          <td><%= answer.description %></td>
          <td><%= answer.created_at.strftime("%I:%M %p, %b %e %Y") %></td>
          <td>Annotations</td>

        </tr>
      <% end %>
      
    </tbody>
  </table>
</div>